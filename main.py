import numpy as np

def iteration(x, t, v, outer):
    for i in range(len(x):
        new_x = np.zeros(2))
        for j in v.shape[0]:
            new_x[i] = x[i] + outer['beta']*[outer['d'][0,0] - v[0,0]]